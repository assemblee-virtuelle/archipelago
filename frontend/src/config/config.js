const config = {
  // Displays import tab when creating resource if it is listed here
  importableResources: [
    "Event",
    "Project",
    "Task",
    "Group",
    "Organization",
    "Idea",
    "Document",
    "Skill",
  ],
};

export default config;
